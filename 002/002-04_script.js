// function calculateRectangleArea(width, height) {
//   return width * height;
// }

// console.log(
//   `Площа прямокутника (звичайна функція): ${calculateRectangleArea(5, 10)}`
// );

let calculateRectangleArea = (width, height) => width * height;
console.log(
  `Площа прямокутника (стрілкова функція): ${calculateRectangleArea(5, 10)}`
);
